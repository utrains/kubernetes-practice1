apiVersion: v1
kind: Pod
metadata:
  name: pod-change-time
spec:
  containers:
  - name: main
    image: alpine
    command: ["/bin/sleep", "999999"]
    securityContext:
      capabilities:
        add:
        - SYS_TIME
